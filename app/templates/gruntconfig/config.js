'use strict';

var config = {
  src: 'src',
  build: '.build',
  buildPort: 8000,
  dist: 'dist',
  distPort: 8003,
  liveReloadPort: 8009,
  templatePort: 8008,
  test: 'test',
  testPort: 8001
};

module.exports = config;
